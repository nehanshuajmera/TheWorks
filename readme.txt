To start your React app with Vite, follow these steps:

Start terminal
Navigate into your project directory in the terminal --> cd client
Run npm install to install the project dependencies --> npm i
Run npm run dev to start the development server --> npm run dev
Your React app should open in your default browser automatically. If not, you can access it at http://localhost:5173.
That's it! React app is now running with Vite.